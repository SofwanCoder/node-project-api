export enum MediaType {
  "IMAGE" = "image",
  "VIDEO" = "video",
  "FILE" = "file",
}

export enum MediaPersistence {
  "TEMPORARY" = "temporary",
  "PERMANENT" = "permanent",
  "HOLD" = "hold",
}

export enum MediaSource {
  "POST" = "post",
  "PROFILE" = "profile",
  "REPORT" = "report",
}
